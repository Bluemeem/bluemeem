// Initialize the client carousel with a 3-second interval
    const clientCarousel = new bootstrap.Carousel(document.querySelector('#clientCarousel'), {
        interval: 2000,
        wrap: true
    });
